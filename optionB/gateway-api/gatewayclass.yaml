# GatewayClass for Traefik
# This tells Kubernetes that Traefik implements the Gateway API

apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: traefik
spec:
  controllerName: traefik.io/gateway-controller
  description: Traefik Gateway Controller

